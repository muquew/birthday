# Copy this file to .env before deployment.
# The frontend is currently built for /xingxing, so keep BASE_PATH=/xingxing.

NODE_ENV=production
PORT=3000
HOST=0.0.0.0
BASE_PATH=/xingxing

# SQLite file path. The app creates it automatically if it does not exist.
DATABASE_PATH=./data/birthday.sqlite

# Used only when the database has no admin user yet.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-this-before-production

# Use a long random value in production.
SESSION_SECRET=replace-with-a-long-random-secret-at-least-24-chars

# Keep production databases empty until you add/import real records.
SEED_SAMPLE_DATA=false
